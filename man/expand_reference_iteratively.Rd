% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multicopy.R
\name{expand_reference_iteratively}
\alias{expand_reference_iteratively}
\title{Iteratively expand ortholog reference set}
\usage{
expand_reference_iteratively(
  orthologs,
  similarity_sp1,
  similarity_sp2,
  multicopy_sp = c("sp2", "sp1", "both"),
  max_copy_number = 2L,
  max_iterations = 5L,
  score_threshold = 0.1
)
}
\arguments{
\item{orthologs}{Data frame with columns \code{gene_sp1}, \code{gene_sp2}, and
optionally \code{type}.}

\item{similarity_sp1, similarity_sp2}{Similarity matrices or TriSimilarity
objects for each species.}

\item{multicopy_sp}{Character. Which species has multi-copy genes:
\code{"sp2"} (default), \code{"sp1"}, or \code{"both"}.}

\item{max_copy_number}{Integer or NULL. Only consider groups with at most
this many copies (default 2L). NULL = no limit.}

\item{max_iterations}{Integer. Maximum number of expansion rounds
(default 5). Iteration stops early if no new pairs are added.}

\item{score_threshold}{Numeric. Minimum preliminary CCS (homeolog score)
required to accept a candidate (default 0.1). Higher values are more
conservative.}
}
\value{
Data frame with the same columns as \code{\link[=collapse_orthologs]{collapse_orthologs()}}, plus
an \code{iteration} column indicating when each pair was added (0 = original
1:1, 1 = first expansion, etc.).
}
\description{
Starts with 1:1 orthologs as the reference set, then iteratively adds
the best multi-copy ortholog representatives. Each iteration re-scores
candidates using the expanded reference from the previous round, so
later additions benefit from a richer reference.
}
\details{
This is useful when few genes have clean 1:1 relationships (e.g.,
polyploid comparisons) and a single-pass collapse is insufficient.

Algorithm:
\enumerate{
\item Extract 1:1 orthologs as initial reference (iteration 0)
\item Score all multi-copy candidates against current reference
\item For each group, select the best candidate above \code{score_threshold}
\item Add selected pairs to the reference set
\item Repeat from step 2 with remaining unassigned groups
\item Stop when no new pairs are added or \code{max_iterations} is reached
}
}
\examples{
\dontrun{
expanded <- expand_reference_iteratively(
  orthologs, sim_sp1, sim_sp2,
  multicopy_sp = "sp2", max_copy_number = 2L,
  max_iterations = 5, score_threshold = 0.1
)

ccs <- calculate_ccs(sim_sp1, sim_sp2, expanded)
}

}
