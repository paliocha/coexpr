% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TriSimilarity-class.R
\name{as.TriSimilarity}
\alias{as.TriSimilarity}
\title{Convert a full matrix to TriSimilarity}
\usage{
as.TriSimilarity(mat)
}
\arguments{
\item{mat}{Numeric matrix. Should be symmetric with gene names as
row/column names.}
}
\value{
A TriSimilarity object.
}
\description{
Extracts the upper triangle of a symmetric matrix and stores it efficiently.
}
\examples{
\dontrun{
# Create symmetric matrix
mat <- matrix(c(1, 0.5, 0.3, 0.5, 1, 0.7, 0.3, 0.7, 1), nrow = 3)
rownames(mat) <- colnames(mat) <- c("A", "B", "C")

# Convert to triangular
tri <- as.TriSimilarity(mat)
}

}
