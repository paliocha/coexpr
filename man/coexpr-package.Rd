% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coexpr-package.R, R/utils.R
\docType{package}
\name{coexpr-package}
\alias{coexpr-package}
\alias{coexpr}
\title{coexpr: Cross-species co-expression network analysis in R}
\description{
Implementation of cross-species comparative transcriptomics using co-expression networks based on Grønvold & Hvidsten. Calculates co-expression correlation scores (CCS) and ortholog rank scores (ORS) to identify conserved gene regulation patterns across species without requiring directly comparable samples.

Implementation of cross-species comparative transcriptomics using co-expression
networks based on Grønvold & Hvidsten. Provides functions to calculate
co-expression correlation scores (CCS) and ortholog rank scores (ORS) to
identify conserved gene regulation patterns across species.
}
\section{Main functions}{

\itemize{
\item \code{\link[=calculate_pcc_mr]{calculate_pcc_mr()}}: Calculate PCC+MR similarity matrix
\item \code{\link[=calculate_ccs]{calculate_ccs()}}: Calculate co-expression correlation score
\item \code{\link[=calculate_ors]{calculate_ors()}}: Calculate ortholog rank score
\item \code{\link[=handle_multicopy_orthologs]{handle_multicopy_orthologs()}}: Handle 1:N and N:M ortholog relationships
}
}

\section{Typical workflow}{

\enumerate{
\item Calculate similarity matrices for each species using \code{\link[=calculate_pcc_mr]{calculate_pcc_mr()}}
\item Process orthologs with \code{\link[=handle_multicopy_orthologs]{handle_multicopy_orthologs()}}
\item Calculate CCS with \code{\link[=calculate_ccs]{calculate_ccs()}}
\item Calculate ORS with \code{\link[=calculate_ors]{calculate_ors()}}
\item Test significance with \code{\link[=test_ors_significance]{test_ors_significance()}}
}
}

\section{Parallelization}{

Both \code{\link[=calculate_pcc_mr]{calculate_pcc_mr()}} and \code{\link[=calculate_ccs]{calculate_ccs()}} support parallel computation
via the \code{n_cores} parameter. This uses the future/furrr framework for
cross-platform parallel processing.

Example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{# Use 4 cores for CCS calculation
ccs_results <- calculate_ccs(sim1, sim2, orthologs, n_cores = 4)

# Use all available cores minus one
n <- parallel::detectCores() - 1
sim <- calculate_pcc_mr(expr, n_cores = n)
}\if{html}{\out{</div>}}

CCS calculation sees the largest speedup (4-8x on typical hardware) since
it is embarrassingly parallel. Similarity matrix calculation benefits from
parallelization primarily for large datasets (>20,000 genes).
}

\author{
\strong{Maintainer}: Martin Paliocha \email{martin.paliocha@nmbu.no}

}
\keyword{internal}
