% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multicopy.R
\name{analyze_paralog_divergence}
\alias{analyze_paralog_divergence}
\title{Analyze expression divergence among paralogs}
\usage{
analyze_paralog_divergence(
  ors_results,
  ccs_threshold = 0.3,
  logors_threshold = 1
)
}
\arguments{
\item{ors_results}{Data frame from \code{calculate_ors()} with at minimum columns
\code{gene_sp1}, \code{gene_sp2}, \code{CCS}, \code{ORS}, and \code{logORS}. Should typically be
generated using \code{all_pairs} strategy and \code{directional = TRUE} in
\code{calculate_ors()}. Must include a \code{type} column with ortholog types.}

\item{ccs_threshold}{Numeric. CCS threshold for classifying divergence.
Pairs with CCS above this are "conserved". Default 0.3.}

\item{logors_threshold}{Numeric. logORS threshold for "highly conserved"
classification. Default 1 (top 10\%).}
}
\value{
A list with two elements:
\describe{
\item{per_group}{Data frame with one row per multi-copy group:
\itemize{
\item \code{group_gene}: The gene with multiple orthologs
\item \code{type}: Ortholog type (1:N, N:1, or N:M)
\item \code{copy_number}: Number of copies in the group
\item \code{primary_gene}: Best-conserved copy (highest CCS)
\item \code{primary_ccs}: CCS of the primary copy
\item \code{secondary_ccs}: CCS of the second-best copy (NA if only 1)
\item \code{delta_ccs}: Difference between primary and secondary CCS
\item \code{mean_ccs}: Mean CCS across all copies
\item \code{classification}: One of "conserved", "partially_diverged",
"fully_diverged"
}
}
\item{by_copy_number}{Data frame summarizing divergence by copy number
(1:2, 1:3, 1:4, ...):
\itemize{
\item \code{copy_number}: Number of copies
\item \code{n_groups}: Number of ortholog groups
\item \code{median_primary_ccs}: Median CCS of primary copy
\item \code{median_delta_ccs}: Median CCS gap between primary and secondary
\item \code{median_logORS}: Median logORS of primary copy
\item \code{pct_conserved}: Percent of groups classified as conserved
}
}
}
}
\description{
Characterizes expression divergence patterns in multi-copy ortholog groups
using CCS and ORS values from an \code{all_pairs} analysis. For each group of
paralogs (1:N, N:1, N:M), identifies the primary (most conserved) copy,
quantifies divergence between copies, and classifies divergence levels.
}
\details{
The paper's key finding is that 1:2, 1:3, 1:4 orthologs show progressively
lower ORS (expression divergence), except for recent WGD duplicates (~13 Mya
in \emph{Glycine max}) which show no divergence.

\strong{Classification rules}:
\itemize{
\item \strong{conserved}: Primary copy CCS >= \code{ccs_threshold} AND
delta_CCS < 0.1 (copies are similarly conserved)
\item \strong{partially_diverged}: Primary copy CCS >= \code{ccs_threshold} AND
delta_CCS >= 0.1 (one copy retains function, others diverge)
\item \strong{fully_diverged}: Primary copy CCS < \code{ccs_threshold}
(all copies have diverged)
}
}
\examples{
\dontrun{
# Run all_pairs CCS and directional ORS
orthologs_all <- handle_multicopy_orthologs(orthologs, strategy = "all_pairs")
ccs <- calculate_ccs(sim_sp1, sim_sp2, orthologs_all)
ors <- calculate_ors(ccs, directional = TRUE)

# Analyze paralog divergence
div <- analyze_paralog_divergence(ors)
div$per_group     # Per-group details
div$by_copy_number  # Summary by copy number
}

}
